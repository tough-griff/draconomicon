require "rails_helper"

# NOTE: is this necessary with such involved controller specs?
RSpec.feature "Creating a character" do
  given(:user) { create(:user) }

  it { skip "needs to be written" }
end
