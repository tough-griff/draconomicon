.flashes#notifications
  - flash.each do |key, value|
    - next unless value.is_a?(String)
    %div{ class: "flash #{key}", id: "flash-#{key}", role: "alertdialog" }
      %span.flash-close{ role: "button", "aria-hidden": true } &times;
      = value
